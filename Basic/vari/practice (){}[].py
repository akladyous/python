class test:
    def __init__(self, stringa):
        # self.valore = stringa
        dstring = {'(': ')', '[': ']', '{': '}'}
        sstring = dict()
        # if (len(stringa))%(len(stringa)-2) == 2:
        for lettera in stringa:
            sstring[lettera]=




t=test('()')